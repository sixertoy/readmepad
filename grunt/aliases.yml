serve:
  - connect
build_documentation:
  - clean:documentation
  - joli_markdown
build_html:
  - clean:html
  - copy:html
  - less
build_server:
  - clean:server
  - copy:server
quick_build:
  - build_html
  - build_server
build:
  - jshint
  - jasmine_node
  - build_html
  - build_server
default:
  - build
  - watch
