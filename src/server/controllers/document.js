/*jslint indent: 4, nomen: true, plusplus: true */
/*globals*/
(function () {

    'use strict';

    /*var Q = require('q'),
        path = require('path'),
        express = require('express'),
        model = require('../models/document'),
        //
        router = express.Router();*/

    /**
     *
     *
     *
     */

    /*router.post('/open', function (req, res) {
        model.openFile(req.body.path, function(err, data){
            if (err) {
                // Facade.send404(res, err);
            } else {
                // Facade.ok(res, data);
            }
        });
    });*/

    /**
     *
     *
     *
     */
    /*router.post('/update', function (req, res) {
        var filepath = req.body.path,
            content = req.body.content;
        model.updateFile(filepath, content, function(err, data){
            if (err) {
                // Facade.send404(res, err);
            } else {
                // Facade.ok(res, data);
            }
        });
    });*/

}());
